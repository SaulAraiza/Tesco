# ----- Conexión a base de datos -------
# Servidor (localhost para entorno local)
DB_SERVER=localhost
# Usuario de MySQL (por defecto: root en XAMPP)
DB_USER_NAME=root
# Contraseña de MySQL (por defecto: vacío en XAMPP, root en MAMP)
DB_PASSWORD=root
# Nombre de la base de datos
DB_NAME=reservaciones_hotel